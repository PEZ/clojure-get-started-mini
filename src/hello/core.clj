(ns hello.core)

;; Load this file with the VS Code command: 
;; **Calva: Load/Evaluate Current File**
;; You should se ”Hello World” printed in the Output Window ->

(comment
  ;; Evaluate the below form (a string) by placing the cursor in
  ;; it and press `Alt/Option+Enter`
  "Hello again"
  
  ;; Evaluate this form too, the same way.
  (println "Hello again, again")
  
  :rcf)

"Hello World!"